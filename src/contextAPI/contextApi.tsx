import { createContext } from "react";

export const ModuleContext = createContext<any>({});
export const MenuContext = createContext<any>({});
export const PaginationContext = createContext<any>({});
export const GetMenuContext = createContext<any>({});